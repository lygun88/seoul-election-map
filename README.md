# Seoul election map - mobile optimized

서울시 행정동별 1위 후보 득표율 지도입니다.

## 변경 사항
- 모바일에서 라벨이 겹치지 않도록 라벨 충돌 방지 적용
- `동명+%`, `%만`, `라벨끄기`, `초기화` 버튼 추가
- 보이지 않는 동은 지도를 터치하면 하단 상세 패널에 표시

## GitHub Pages
`index.html`이 저장소 최상위에 오도록 업로드한 뒤, Settings > Pages에서 main / root를 선택하세요.
